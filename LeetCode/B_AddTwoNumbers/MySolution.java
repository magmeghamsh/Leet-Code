package LeetCode.B_AddTwoNumbers;

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) { val = x; }
 * }
 *
 * Pseudo code:
 * Step 1: add each value in the lists and store the carry and add it to the next element
 * Step 2: Loop until list1 is not null OR list2 not null OR Carry is Null
 * */

public class MySolution {

    ListNode prev = new ListNode(0);
}
